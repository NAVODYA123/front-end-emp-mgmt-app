import { createTheme } from '@mui/material/styles'

const EmpAppTheme = createTheme({
  typography: {
    h6: {
      fontSize: '18px',
    },
    body1: {
      fontSize: '14px',
    },
  },
})

export default EmpAppTheme
